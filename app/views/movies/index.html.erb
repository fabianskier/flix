<ul>
    <% @movies.each do |movie| %>
    <li>
        <strong><%= movie.title %></strong> (<%= movie.rating %>): <%= movie.total_gross %>
    </li>
    <p>
        <%= movie.description %>
    </p>
    <p>
        <%= movie.released_on %>
    </p>
    <% end %>
</ul>